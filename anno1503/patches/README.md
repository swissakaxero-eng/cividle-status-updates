# Patch payloads

Future Anno 1503 trainer updates are published here as versioned Python patch payloads.
The stable channel manifest contains the URL and SHA-256 for each payload.
Payloads are applied only to the trainer's local source tree and are followed by a local PyInstaller rebuild.
